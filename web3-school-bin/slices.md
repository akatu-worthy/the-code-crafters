## Go Slices

 A slice is also similar to an array but it's more powerful and flexible, it can also be used to stroe multiple values of the same type in a single varaible. Unlike arrays, the length of a slice can grow and shrink as you see fit.

 Example: 
 slice_name := []datatype{values} 